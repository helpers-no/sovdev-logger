# DevContainer Additions Manual

> **Auto-generated documentation for all installation scripts**  
> Last updated: 2025-11-28 23:55:22  
> Generated by: `.devcontainer/manage/generate-manual.sh`

This manual provides comprehensive documentation for all available installation scripts in the devcontainer additions.

## Categories

This manual is organized into the following categories:

### Development Tools

Programming language development environments and tools (Python, TypeScript, Go, Rust, C#, Java, PHP)

**Scripts in this category:**        7

### AI & Machine Learning Tools

AI and machine learning development tools (Claude Code, etc.)

**Scripts in this category:**        1

### Cloud & Infrastructure Tools

Cloud platform tools and SDKs (Azure, AWS, GCP)

**Scripts in this category:**        1

### Data & Analytics Tools

Data analysis, visualization, and data engineering tools (Jupyter, pandas, DBT)

**Scripts in this category:**        1

### Infrastructure & Configuration

Infrastructure as Code, configuration management, and DevOps tools (Ansible, Kubernetes, Terraform)

**Scripts in this category:**        5

---## Table of Contents

- [Development Tools](#development-tools)
- [AI & Machine Learning Tools](#ai---machine-learning-tools)
- [Cloud & Infrastructure Tools](#cloud---infrastructure-tools)
- [Data & Analytics Tools](#data---analytics-tools)
- [Infrastructure & Configuration](#infrastructure---configuration)

---[23:55:22] ℹ️    Generating section: Development Tools
[23:55:22] ℹ️      Processed 7 scripts in category
## Development Tools

### C# Development Tools

**Script ID:** `dev-csharp`  
**Script:** `install-dev-csharp.sh`  
**Command:** `.devcontainer/additions/install-dev-csharp.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-csharp-20251128-235522.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 C# Development Tools
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-csharp
Name:         C# Development Tools
Category:     LANGUAGE_DEV, Development Tools
Description:  Complete .NET 8.0 development environment with Azure Functions, Bicep IaC, storage emulation, and VS Code extensions

Check Command: [ -f /Users/terje.christensen/.dotnet/dotnet ] || [ -f /usr/bin/dotnet ] || command -v dotnet >/dev/null 2>&1

Usage:
  install-dev-csharp.sh              # Install
  install-dev-csharp.sh --help       # Show this help
  install-dev-csharp.sh --uninstall  # Uninstall

VS Code Extensions:
  - C# Dev Kit (ms-dotnettools.csdevkit) - Complete C# development experience
  - C# (ms-dotnettools.csharp) - C# language support
  - .NET Runtime (ms-dotnettools.vscode-dotnet-runtime) - .NET runtime support

Verification Commands:
  - if command -v dotnet >/dev/null; then echo "✅ .NET SDK: $(dotnet --version)"; else echo "❌ .NET SDK not found"; fi
  - if dotnet --list-sdks | grep -q "8.0"; then echo "✅ .NET SDK 8.0 is installed"; else echo "❌ .NET SDK 8.0 not found"; fi
  - if command -v func >/dev/null; then echo "✅ Azure Functions Core Tools: $(func --version)"; else echo "❌ Azure Functions Core Tools not found"; fi
  - if code --list-extensions | grep -q ms-dotnettools.csdevkit; then echo "✅ C# Dev Kit is installed"; else echo "❌ C# Dev Kit not installed"; fi

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Go Runtime & Development Tools

**Script ID:** `dev-golang`  
**Script:** `install-dev-golang.sh`  
**Command:** `.devcontainer/additions/install-dev-golang.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-golang-20251128-235522.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

/Users/terje.christensen/learn/redcross-public/sovdev-logger/.devcontainer/additions/lib/categories.sh: line 30: CATEGORY_TABLE: readonly variable
/Users/terje.christensen/learn/redcross-public/sovdev-logger/.devcontainer/additions/lib/categories.sh: line 141: CATEGORY_LANGUAGE_DEV: readonly variable
/Users/terje.christensen/learn/redcross-public/sovdev-logger/.devcontainer/additions/lib/categories.sh: line 142: CATEGORY_AI_TOOLS: readonly variable
/Users/terje.christensen/learn/redcross-public/sovdev-logger/.devcontainer/additions/lib/categories.sh: line 143: CATEGORY_CLOUD_TOOLS: readonly variable
/Users/terje.christensen/learn/redcross-public/sovdev-logger/.devcontainer/additions/lib/categories.sh: line 144: CATEGORY_DATA_ANALYTICS: readonly variable
/Users/terje.christensen/learn/redcross-public/sovdev-logger/.devcontainer/additions/lib/categories.sh: line 145: CATEGORY_INFRA_CONFIG: readonly variable

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 Go Runtime & Development Tools
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-golang
Name:         Go Runtime & Development Tools
Category:     LANGUAGE_DEV, Development Tools
Description:  Installs Go runtime, common tools, and VS Code extensions for Go development.

Check Command: [ -f /usr/local/go/bin/go ] || [ -f /usr/bin/go ] || command -v go >/dev/null 2>&1

Usage:
  install-dev-golang.sh                    # Install (default version)
  install-dev-golang.sh --version X.Y.Z    # Install specific Go version (e.g., 1.21.0)
  install-dev-golang.sh --help             # Show this help
  install-dev-golang.sh --uninstall        # Uninstall

VS Code Extensions:
  - Go (golang.go) - Core Go language support
  - Go Test Explorer (premparihar.gotestexplorer) - Test runner and debugger
  - Protocol Buffers (zxh404.vscode-proto3) - Protocol Buffer support

Verification Commands:
  - command -v go >/dev/null && go version || echo '❌ Go not found'
  - go env || echo '❌ Failed to get Go environment'
  - [ -f go.mod ] && (go list -m all || echo '❌ Failed to list Go modules') || echo 'ℹ️  No go.mod found - skipping module list'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Java Runtime & Development Tools

**Script ID:** `dev-java`  
**Script:** `install-dev-java.sh`  
**Command:** `.devcontainer/additions/install-dev-java.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-java-20251128-235522.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

/Users/terje.christensen/learn/redcross-public/sovdev-logger/.devcontainer/additions/lib/categories.sh: line 30: CATEGORY_TABLE: readonly variable
/Users/terje.christensen/learn/redcross-public/sovdev-logger/.devcontainer/additions/lib/categories.sh: line 141: CATEGORY_LANGUAGE_DEV: readonly variable
/Users/terje.christensen/learn/redcross-public/sovdev-logger/.devcontainer/additions/lib/categories.sh: line 142: CATEGORY_AI_TOOLS: readonly variable
/Users/terje.christensen/learn/redcross-public/sovdev-logger/.devcontainer/additions/lib/categories.sh: line 143: CATEGORY_CLOUD_TOOLS: readonly variable
/Users/terje.christensen/learn/redcross-public/sovdev-logger/.devcontainer/additions/lib/categories.sh: line 144: CATEGORY_DATA_ANALYTICS: readonly variable
/Users/terje.christensen/learn/redcross-public/sovdev-logger/.devcontainer/additions/lib/categories.sh: line 145: CATEGORY_INFRA_CONFIG: readonly variable

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 Java Runtime & Development Tools
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-java
Name:         Java Runtime & Development Tools
Category:     LANGUAGE_DEV, Development Tools
Description:  Installs Java JDK, Maven, Gradle, and VS Code extensions for Java development.

Check Command: [ -f /usr/bin/java ] || [ -f /usr/lib/jvm/*/bin/java ] || command -v java >/dev/null 2>&1

Usage:
  install-dev-java.sh                # Install (default version)
  install-dev-java.sh --version X    # Install specific Java version (e.g., 11, 17, 21)
  install-dev-java.sh --help         # Show this help
  install-dev-java.sh --uninstall    # Uninstall

VS Code Extensions:
  - Language Support for Java (redhat.java) - Core Java language support
  - Debugger for Java (vscjava.vscode-java-debug) - Debugging support
  - Test Runner for Java (vscjava.vscode-java-test) - Test runner and debugger
  - Maven for Java (vscjava.vscode-maven) - Maven project support
  - Dependency Viewer (vscjava.vscode-java-dependency) - View and manage dependencies
  - Extension Pack for Java (vscjava.vscode-java-pack) - Collection of popular Java extensions

Verification Commands:
  - command -v java >/dev/null && java -version || echo '❌ Java not found'
  - command -v javac >/dev/null && javac -version || echo '❌ Java compiler not found'
  - command -v mvn >/dev/null && mvn --version || echo '❌ Maven not found'
  - command -v gradle >/dev/null && gradle --version || echo '❌ Gradle not found'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### PHP Laravel Development Tools

**Script ID:** `dev-php-laravel`  
**Script:** `install-dev-php-laravel.sh`  
**Command:** `.devcontainer/additions/install-dev-php-laravel.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-php-laravel-20251128-235522.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 PHP Laravel Development Tools
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-php-laravel
Name:         PHP Laravel Development Tools
Category:     LANGUAGE_DEV, Development Tools
Description:  Installs PHP 8.4, Composer, Laravel installer, and sets up Laravel development environment

Check Command: ([ -f /usr/bin/php ] || command -v php >/dev/null 2>&1) && ([ -f /usr/local/bin/composer ] || command -v composer >/dev/null 2>&1) && ([ -f /Users/terje.christensen/.composer/vendor/bin/laravel ] || command -v laravel >/dev/null 2>&1)

Usage:
  install-dev-php-laravel.sh              # Install
  install-dev-php-laravel.sh --help       # Show this help
  install-dev-php-laravel.sh --uninstall  # Uninstall

VS Code Extensions:
  - PHP Intelephense (bmewburn.vscode-intelephense-client) - Advanced PHP language support with IntelliSense
  - PHP Debug (xdebug.php-debug) - Debug PHP applications using Xdebug
  - PHP DocBlocker (neilbrayfield.php-docblocker) - Automatically generate PHPDoc comments
  - Composer (ikappas.composer) - Composer dependency manager integration
  - PHP Namespace Resolver (mehedidracula.php-namespace-resolver) - Auto-import and resolve PHP namespaces
  - Laravel Blade Snippets (onecentlin.laravel-blade) - Blade syntax highlighting and snippets
  - Laravel Artisan (ryannaddy.laravel-artisan) - Run Laravel Artisan commands from VS Code
  - SQLTools (mtxr.sqltools) - Database management and SQL query tool
  - REST Client (humao.rest-client) - Send HTTP requests and view responses directly in VS Code

Verification Commands:
  - command -v php >/dev/null && php --version | head -n 1 || echo '❌ PHP not found'
  - command -v composer >/dev/null && composer --version | head -n 1 || echo '❌ Composer not found'
  - command -v laravel >/dev/null && laravel --version || echo '❌ Laravel installer not found'
  - php -m | grep -q 'mbstring' && echo '✅ PHP mbstring extension loaded' || echo '❌ PHP mbstring extension missing'
  - php -m | grep -q 'curl' && echo '✅ PHP curl extension loaded' || echo '❌ PHP curl extension missing'
  - php -m | grep -q 'sqlite3' && echo '✅ PHP SQLite extension loaded' || echo '❌ PHP SQLite extension missing'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Python Development Tools

**Script ID:** `dev-python`  
**Script:** `install-dev-python.sh`  
**Command:** `.devcontainer/additions/install-dev-python.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-python-20251128-235522.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 Python Development Tools
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-python
Name:         Python Development Tools
Category:     LANGUAGE_DEV, Development Tools
Description:  Installs Python 3.11+, pip, venv, and essential development tools

Check Command: [ -f /usr/local/bin/python3 ] || [ -f /usr/bin/python3 ] || command -v python3 >/dev/null 2>&1

Usage:
  install-dev-python.sh              # Install Python development environment
  install-dev-python.sh --help       # Show this help
  install-dev-python.sh --uninstall  # Uninstall Python packages (system Python remains)
  install-dev-python.sh --debug      # Install with debug output

System Packages (APT):
  - python3
  - python3-pip
  - python3-venv
  - python3-dev
  - python3-setuptools
  - python3-wheel
  - build-essential
  - libffi-dev
  - libssl-dev

Python Packages (pip):
  - pip
  - setuptools
  - wheel
  - virtualenv
  - requests
  - pytest
  - black
  - flake8
  - mypy

VS Code Extensions:
  - Python (ms-python.python) - Python language support
  - Pylance (ms-python.vscode-pylance) - Python language server
  - Black Formatter (ms-python.black-formatter) - Python code formatter
  - Flake8 (ms-python.flake8) - Python linter
  - Mypy (ms-python.mypy-type-checker) - Python type checker

Verification Commands:
  - command -v python3 >/dev/null && python3 --version || echo '❌ Python not found'
  - command -v pip3 >/dev/null && pip3 --version || echo '❌ pip not found'
  - python3 -c 'import venv' 2>/dev/null && echo '✅ venv module available' || echo '❌ venv module not available'
  - grep -q 'alias python=' ~/.bashrc && echo '✅ Python aliases configured' || echo 'ℹ️  Python aliases not yet active (restart shell)'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Rust Development Tools

**Script ID:** `dev-rust`  
**Script:** `install-dev-rust.sh`  
**Command:** `.devcontainer/additions/install-dev-rust.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-rust-20251128-235522.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 Rust Development Tools
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-rust
Name:         Rust Development Tools
Category:     LANGUAGE_DEV, Development Tools
Description:  Installs Rust (latest stable via rustup), cargo, and sets up Rust development environment

Check Command: [ -f /Users/terje.christensen/.cargo/bin/rustc ] || command -v rustc >/dev/null 2>&1

Usage:
  install-dev-rust.sh              # Install
  install-dev-rust.sh --help       # Show this help
  install-dev-rust.sh --uninstall  # Uninstall

VS Code Extensions:
  - Rust Analyzer (rust-lang.rust-analyzer) - Rust language support with rust-analyzer
  - CodeLLDB (vadimcn.vscode-lldb) - Native debugger for Rust
  - Crates (serayuzgur.crates) - Helps manage Rust dependencies

Verification Commands:
  - command -v rustc >/dev/null && echo '✅ Rust compiler is available' || echo '❌ Rust compiler not found'
  - command -v cargo >/dev/null && echo '✅ Cargo is available' || echo '❌ Cargo not found'
  - command -v rustup >/dev/null && echo '✅ Rustup is available' || echo '❌ Rustup not found'
  - test -d $HOME/.cargo && echo '✅ Cargo directory exists' || echo '❌ Cargo directory not found'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### TypeScript Development Tools

**Script ID:** `dev-typescript`  
**Script:** `install-dev-typescript.sh`  
**Command:** `.devcontainer/additions/install-dev-typescript.sh --help`

[23:55:22] ❌     Failed to run install-dev-typescript.sh --help
---[23:55:22] ℹ️    Generating section: AI & Machine Learning Tools
[23:55:22] ℹ️      Processed 1 scripts in category
## AI & Machine Learning Tools

### Claude Code

**Script ID:** `dev-ai-claudecode`  
**Script:** `install-dev-ai-claudecode.sh`  
**Command:** `.devcontainer/additions/install-dev-ai-claudecode.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-ai-claudecode-20251128-235522.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 Claude Code
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-ai-claudecode
Name:         Claude Code
Category:     AI_TOOLS, AI & Machine Learning Tools
Description:  Installs Claude Code, Anthropic's terminal-based AI coding assistant with agentic capabilities and LSP integration

Check Command: [ -f /Users/terje.christensen/.local/bin/claude ] || [ -f /usr/local/bin/claude ] || command -v claude >/dev/null 2>&1

Usage:
  install-dev-ai-claudecode.sh              # Install Claude Code
  install-dev-ai-claudecode.sh --help       # Show this help
  install-dev-ai-claudecode.sh --uninstall  # Uninstall Claude Code
  install-dev-ai-claudecode.sh --debug      # Install with debug output

System Packages (APT):
  - curl

Node.js Packages (NPM):
  - @anthropic-ai/claude-code

Verification Commands:
  - command -v claude >/dev/null && echo ' Claude Code binary is available' || echo 'L Claude Code binary not found'
  - test -L /home/vscode/.claude-code-env && echo ' Environment config symlink exists' || echo '�  Environment config symlink not found'
  - test -d /workspace/.devcontainer.secrets/env-vars && echo ' Environment directory exists in .devcontainer.secrets/' || echo 'L Environment directory not found'
  - test -d /workspace/.claude/skills && echo ' Skills directory exists' || echo '�  Skills directory not found'
  - grep -q '.devcontainer.secrets/' /workspace/.gitignore && echo ' .devcontainer.secrets/ is gitignored' || echo 'L .devcontainer.secrets/ NOT gitignored (SECURITY RISK!)'
  - grep -q 'Claude Code environment' /home/vscode/.bashrc && echo ' Environment loading added to bashrc' || echo '�  bashrc not configured'
  - claude --version >/dev/null 2>&1 && echo ' Claude Code is functional' || echo '�  Claude Code installed'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---[23:55:22] ℹ️    Generating section: Cloud & Infrastructure Tools
[23:55:23] ℹ️      Processed 1 scripts in category
## Cloud & Infrastructure Tools

### Azure Development Tools

**Script ID:** `tool-azure`  
**Script:** `install-tool-azure.sh`  
**Command:** `.devcontainer/additions/install-tool-azure.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-tool-azure-20251128-235523.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 Azure Development Tools
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           tool-azure
Name:         Azure Development Tools
Category:     CLOUD_TOOLS, Cloud & Infrastructure Tools
Description:  Installs Azure CLI and Azure VS Code extensions for comprehensive Azure cloud development

Check Command: [ -f /usr/bin/az ] || [ -f /usr/local/bin/az ] || command -v az >/dev/null 2>&1

Usage:
  install-tool-azure.sh              # Install Azure development tools
  install-tool-azure.sh --help       # Show this help
  install-tool-azure.sh --uninstall  # Uninstall Azure tools
  install-tool-azure.sh --debug      # Install with debug output

System Packages (APT):
  - ca-certificates
  - curl
  - apt-transport-https
  - lsb-release
  - gnupg

VS Code Extensions:
  - Azure Account (ms-vscode.azure-account) - Azure account management
  - Azure Resources (ms-azuretools.vscode-azureresourcegroups) - View and manage Azure resources
  - Azure App Service (ms-azuretools.vscode-azureappservice) - Deploy to Azure App Service
  - Azure Functions (ms-azuretools.vscode-azurefunctions) - Create and deploy Azure Functions
  - Azure Storage (ms-azuretools.vscode-azurestorage) - Manage Azure Storage accounts

Verification Commands:
  - command -v az >/dev/null && az version --output json | head -10 || echo 'L Azure CLI not found'
  - az account list-locations --output table --query '[0:3]' 2>/dev/null || echo '9  Not logged in to Azure (run: az login)'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---[23:55:23] ℹ️    Generating section: Data & Analytics Tools
[23:55:23] ℹ️      Processed 1 scripts in category
## Data & Analytics Tools

### Data & Analytics Tools

**Script ID:** `tool-dataanalytics`  
**Script:** `install-tool-dataanalytics.sh`  
**Command:** `.devcontainer/additions/install-tool-dataanalytics.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-tool-dataanalytics-20251128-235523.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 Data & Analytics Tools
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           tool-dataanalytics
Name:         Data & Analytics Tools
Category:     DATA_ANALYTICS, Data & Analytics Tools
Description:  Installs Python data analysis libraries, Jupyter notebooks, and related VS Code extensions

Check Command: [ -f /usr/local/bin/jupyter ] || [ -f /Users/terje.christensen/.local/bin/jupyter ] || command -v jupyter >/dev/null 2>&1

Usage:
  install-tool-dataanalytics.sh              # Install data analytics tools
  install-tool-dataanalytics.sh --help       # Show this help
  install-tool-dataanalytics.sh --uninstall  # Uninstall analytics tools
  install-tool-dataanalytics.sh --debug      # Install with debug output

Python Packages (pip):
  - pandas
  - numpy
  - matplotlib
  - seaborn
  - scikit-learn
  - jupyter
  - jupyterlab
  - notebook
  - dbt-core
  - dbt-postgres

Verification Commands:
  - python --version || echo 'L Python not found'
  - pip --version || echo 'L pip not found'
  - python -c 'import pandas' 2>/dev/null && echo ' pandas is installed' || echo 'L pandas not found'
  - python -c 'import numpy' 2>/dev/null && echo ' numpy is installed' || echo 'L numpy not found'
  - python -c 'import matplotlib' 2>/dev/null && echo ' matplotlib is installed' || echo 'L matplotlib not found'
  - python -c 'import seaborn' 2>/dev/null && echo ' seaborn is installed' || echo 'L seaborn not found'
  - python -c 'import sklearn' 2>/dev/null && echo ' scikit-learn is installed' || echo 'L scikit-learn not found'
  - jupyter --version >/dev/null 2>&1 && echo ' jupyter is installed' || echo 'L jupyter not found'
  - dbt --version >/dev/null 2>&1 && echo ' dbt is installed' || echo 'L dbt not found'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---[23:55:23] ℹ️    Generating section: Infrastructure & Configuration
[23:55:23] ℹ️      Processed 5 scripts in category
## Infrastructure & Configuration

### PowerShell

**Script ID:** `dev-powershell`  
**Script:** `install-dev-powershell.sh`  
**Command:** `.devcontainer/additions/install-dev-powershell.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-powershell-20251128-235523.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 PowerShell
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-powershell
Name:         PowerShell
Category:     INFRA_CONFIG, Infrastructure & Configuration
Description:  Installs PowerShell runtime and modules for Azure and Microsoft Graph development

Check Command: [ -f /usr/bin/pwsh ] || [ -f /opt/microsoft/powershell/7/pwsh ] || command -v pwsh >/dev/null 2>&1

Usage:
  install-dev-powershell.sh              # Install PowerShell
  install-dev-powershell.sh --help       # Show this help
  install-dev-powershell.sh --uninstall  # Uninstall PowerShell
  install-dev-powershell.sh --debug      # Install with debug output

System Packages (APT):
  - curl
  - ca-certificates
  - gnupg

PowerShell Modules:
  - Az
  - Microsoft.Graph
  - PSScriptAnalyzer

VS Code Extensions:
  - PowerShell (ms-vscode.powershell) - PowerShell language support and debugging

Verification Commands:
  - command -v pwsh >/dev/null && pwsh -Version || echo 'L PowerShell not found'
  - pwsh -NoProfile -NonInteractive -Command "Get-Module -ListAvailable Az* | Select-Object Name, Version" 2>/dev/null || echo '�  Az module not found'
  - pwsh -NoProfile -NonInteractive -Command "Get-Module -ListAvailable Microsoft.Graph | Select-Object Name, Version" 2>/dev/null || echo '�  Microsoft.Graph module not found'
  - pwsh -NoProfile -NonInteractive -Command "Get-Module -ListAvailable PSScriptAnalyzer | Select-Object Name, Version" 2>/dev/null || echo '�  PSScriptAnalyzer not found'
  - code --list-extensions 2>/dev/null | grep -q 'ms-vscode.powershell' && echo ' PowerShell extension is installed' || echo '�  PowerShell extension is not installed'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Nginx Reverse Proxy

**Script ID:** `srv-nginx`  
**Script:** `install-srv-nginx.sh`  
**Command:** `.devcontainer/additions/install-srv-nginx.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-srv-nginx-20251128-235523.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 Nginx Reverse Proxy
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           srv-nginx
Name:         Nginx Reverse Proxy
Category:     INFRA_CONFIG, Infrastructure & Configuration
Description:  Install nginx as reverse proxy for Claude Code � LiteLLM with Host header injection

Check Command: command -v nginx >/dev/null 2>&1

Usage:
  install-srv-nginx.sh              # Install nginx
  install-srv-nginx.sh --help       # Show this help
  install-srv-nginx.sh --uninstall  # Uninstall nginx
  install-srv-nginx.sh --debug      # Install with debug output

System Packages (APT):
  - nginx-light

Verification Commands:
  - command -v nginx >/dev/null && echo ' Nginx binary is available' || echo 'L Nginx binary not found'
  - nginx -v 2>&1 || echo 'L Nginx version check failed'
  - test -d /etc/nginx && echo ' Nginx configuration directory exists' || echo 'L Nginx configuration directory not found'
  - test -f /etc/nginx/nginx.conf && echo ' Nginx main configuration file exists' || echo 'L Nginx configuration file not found'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### OpenTelemetry Monitoring

**Script ID:** `srv-otel`  
**Script:** `install-srv-otel-monitoring.sh`  
**Command:** `.devcontainer/additions/install-srv-otel-monitoring.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-srv-otel-monitoring-20251128-235523.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 OpenTelemetry Monitoring
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           srv-otel
Name:         OpenTelemetry Monitoring
Category:     INFRA_CONFIG, Infrastructure & Configuration
Description:  Install OpenTelemetry Collector for devcontainer monitoring when connected to our network

Check Command: ([ -f /usr/bin/otelcol-contrib ] || command -v otelcol-contrib >/dev/null 2>&1) && ([ -f /usr/local/bin/script_exporter ] || command -v script_exporter >/dev/null 2>&1)

Usage:
  install-srv-otel-monitoring.sh              # Install OpenTelemetry Collector
  install-srv-otel-monitoring.sh --help       # Show this help
  install-srv-otel-monitoring.sh --uninstall  # Uninstall OpenTelemetry Collector
  install-srv-otel-monitoring.sh --debug      # Install with debug output

System Packages (APT):
  - curl
  - ca-certificates

Verification Commands:
  - command -v otelcol-contrib >/dev/null && echo ' OpenTelemetry Collector is available' || echo '�  OpenTelemetry Collector not found'
  - command -v script_exporter >/dev/null && echo ' Script exporter is available' || echo '�  Script exporter not found'
  - test -d /etc/otelcol-contrib && echo ' OpenTelemetry Collector configuration directory exists' || echo '�  Configuration directory not found'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Configuration Tools

**Script ID:** `tool-iac`  
**Script:** `install-tool-iac.sh`  
**Command:** `.devcontainer/additions/install-tool-iac.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-tool-iac-20251128-235523.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 Configuration Tools
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           tool-iac
Name:         Configuration Tools
Category:     INFRA_CONFIG, Infrastructure & Configuration
Description:  Installs tools and extensions for Infrastructure as Code (IaC) and configuration management (Ansible)

Check Command: [ -f /usr/local/bin/ansible ] || [ -f /usr/bin/ansible ] || command -v ansible >/dev/null 2>&1

Usage:
  install-tool-iac.sh              # Install IaC tools
  install-tool-iac.sh --help       # Show this help
  install-tool-iac.sh --uninstall  # Uninstall IaC tools
  install-tool-iac.sh --debug      # Install with debug output

System Packages (APT):
  - ansible
  - ansible-lint

Verification Commands:
  - command -v ansible >/dev/null && ansible --version | head -n1 || echo 'L ansible not found'
  - command -v ansible-lint >/dev/null && ansible-lint --version || echo 'L ansible-lint not found'
  - code --list-extensions | grep -q redhat.ansible && echo ' Ansible extension is installed' || echo 'L Ansible extension is not installed'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Kubernetes Development Tools

**Script ID:** `tool-kubernetes`  
**Script:** `install-tool-kubernetes.sh`  
**Command:** `.devcontainer/additions/install-tool-kubernetes.sh --help`

```
📝 Logging to: /tmp/devcontainer-install/install-tool-kubernetes-20251128-235523.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 Kubernetes Development Tools
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           tool-kubernetes
Name:         Kubernetes Development Tools
Category:     INFRA_CONFIG, Infrastructure & Configuration
Description:  Installs kubectl, k9s, helm and sets up .devcontainer.secrets folder for kubeconfig

Check Command: command -v kubectl >/dev/null 2>&1 || command -v k9s >/dev/null 2>&1 || command -v helm >/dev/null 2>&1

Usage:
  install-tool-kubernetes.sh              # Install Kubernetes development tools
  install-tool-kubernetes.sh --help       # Show this help
  install-tool-kubernetes.sh --uninstall  # Uninstall Kubernetes tools
  install-tool-kubernetes.sh --debug      # Install with debug output

System Packages (APT):
  - curl
  - ca-certificates

VS Code Extensions:
  - Kubernetes (ms-kubernetes-tools.vscode-kubernetes-tools) - Develop, deploy and debug Kubernetes applications
  - YAML (redhat.vscode-yaml) - YAML language support with Kubernetes schema

Verification Commands:
  - command -v kubectl >/dev/null && kubectl version --client || echo 'L kubectl not found'
  - command -v k9s >/dev/null && k9s version -s || echo 'L k9s not found'
  - command -v helm >/dev/null && helm version --short || echo 'L helm not found'
  - test -f /workspace/.devcontainer.secrets/.kube/config && echo ' kubeconfig found' || echo '�  kubeconfig not found'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---
---

## About This Manual

This manual was automatically generated from the `--help` output of all installation scripts.

**To regenerate this manual:**
```bash
cd .devcontainer/manage
./generate-manual.sh
```

**To see help for a specific script:**
```bash
cd .devcontainer/additions
./install-<name>.sh --help
```

For more information about the devcontainer toolbox, see the main README.

